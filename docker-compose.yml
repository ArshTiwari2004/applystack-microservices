version: '3.8'

services:
  backend:
    build: ./backend
    container_name: applystack-backend
    ports:
      - "8001:8001"
    environment:
      - NODE_ENV=production
      - PORT=8001
      - MONGO_URL=mongodb://mongo:27017
      - DB_NAME=applystack
      - JWT_SECRET=${JWT_SECRET:-your-secret-key-change-in-production}
      - REDIS_URL=redis://redis:6379
      - RABBITMQ_URL=amqp://applystack:applystack123@rabbitmq:5672
      - CORS_ORIGINS=http://localhost:3000
    depends_on:
      mongo:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - applystack-network

  mongo:
    image: mongo:7
    container_name: applystack-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - applystack-network

  redis:
    image: redis:7-alpine
    container_name: applystack-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - applystack-network

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: applystack-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      - RABBITMQ_DEFAULT_USER=applystack
      - RABBITMQ_DEFAULT_PASS=applystack123
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - applystack-network

volumes:
  mongo_data:
  redis_data:
  rabbitmq_data:

networks:
  applystack-network:
    driver: bridge